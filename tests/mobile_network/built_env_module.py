"""Test Built Environment Module
"""

import pytest
from digital_comms.mobile_network.built_env_module.py import find_line_of_sight

# @pytest.mark.parametrize("x_transmitter, y_transmitter, x_receiver, y_receiver, expected", [
#     (0.124896, 52.215965, 0.133939, 52.215263, INSERT_EXPECTED_LOS),
# ])

# def test_eval(x_transmitter, y_transmitter, x_receiver, y_receiver, expected):
#     assert (find_line_of_sight(x_transmitter, y_transmitter, x_receiver, y_receiver)) == expected

print('complete')